# 利润宝文档整理实施计划

**目标：** 将项目文档按使用场景分层，减少根目录与 `docs/` 的混放，同时保留全部历史资料和可追溯链接。

**信息架构：** 根目录只保留项目入口、协作约束、版本变更与私有声明；`docs/` 以“管理、使用与发布、产品与技术、验收与记录”四类组织，并通过文档索引提供唯一导航入口。

## 约束

- 不删除现有 Markdown 内容，只调整位置和内部链接。
- 新增 Markdown 文件遵循 `CO_*_WB-CO-TR-日期时间` 命名规范。
- 保持 `AGENTS.md` 在项目根目录，且不修改其项目护栏内容。

## 执行清单

- [x] 建立四类文档目录，移动现有 `docs/` 文件至对应类别。
- [x] 新增文档索引，说明每份资料的读者、用途与位置。
- [x] 更新 `README.md` 的文档入口和目录树。
- [x] 更新发布说明、开发方案中的相对路径。
- [x] 用文件清单和全文检索确认没有旧路径残留。
