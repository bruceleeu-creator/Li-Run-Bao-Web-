# 利润宝 · 依赖清单（S1-S9 完整版 + 模板版 T6）
# ---------------------------------------------------------------------------
# 运行时硬约束（2026-08 确认）：
#   - Python 3.11+
#   - 核心计算仅依赖标准库 + openpyxl，未配置大模型时完整离线可用
#   - Tk 桌面端已移除（2026-08），Web 为唯一入口
#
# 第三方运行库（Excel / 图表 / 报告导出 / PDF 解析）
openpyxl>=3.1
matplotlib>=3.7
python-docx>=0.8.11
python-pptx>=0.6.21
pdfplumber>=0.11
pypdfium2>=5.0
rapidocr_onnxruntime>=1.4
reportlab>=4.0
requests>=2.28
#
# 本机 Web 端（唯一入口，仅 127.0.0.1）
fastapi>=0.110
uvicorn>=0.27
httpx2>=2.0
python-multipart>=0.0.9
#
# 可选大模型增强（OpenAI 兼容接口），未配置时由规则引擎兜底，离线闭环不依赖
# API Key 持久化于 .ai_config.json（用户选择重启免重输），已 gitignore 不入库
# ---------------------------------------------------------------------------
# 开发 / 测试期依赖
pytest>=7.4
