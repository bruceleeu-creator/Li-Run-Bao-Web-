# 协同看板 · 服务器环境配置模板
# 复制为 collab_board/.env 并填入真实值（chmod 600；.env 已被 gitignore，绝不入库）
#
#   cp .env.example .env
#   # 生成随机密码：python -c "import secrets; print(secrets.token_urlsafe(24))"
#   # JWT_SECRET 必须 ≥32 位随机

# PostgreSQL 数据库密码（docker-compose 内 db 容器与 app 连接串共用）
BOARD_DB_PASSWORD=<改为32位以上随机串>

# JWT 签名密钥（≥32 位随机；泄露=任何人可伪造登录态，务必保密）
JWT_SECRET=<改为32位以上随机串>
